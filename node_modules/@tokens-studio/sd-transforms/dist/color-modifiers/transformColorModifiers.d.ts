import { DesignToken } from 'style-dictionary';
/**
 * Helper: Transforms color tokens with tokens studio color modifiers
 */
export declare function transformColorModifiers(token: DesignToken): string | undefined;
