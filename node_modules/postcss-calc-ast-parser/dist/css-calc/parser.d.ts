import * as AST from "../types/ast";
import { Tokenizer } from "./tokenizer";
import { Options } from "../types/options";
export declare class Parser {
    private tokenizer;
    private root;
    private tokens;
    private errors;
    private rescans;
    constructor(tokenizer: Tokenizer, _options?: Options);
    parse(): AST.Root;
    private reportParseError;
    private processExpressions;
    private checkAndMergeMathExpr;
    private processPunctuator;
    private processFunction;
    private createNestedStateContainer;
    private mergeMathExpr;
    private postStack;
    private scan;
    private back;
}
